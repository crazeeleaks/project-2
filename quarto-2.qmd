---
title: "Quarto 2"
author: Imre Huss
format: html
execute:
  echo: false
  message: false
  warning: false
---

```{r}
suppressPackageStartupMessages(library(tidyverse))
```

```{r}
library(ggplot2)
library(dplyr)

class_summary <- mpg %>%
  group_by(class) %>%
  summarise(avg_hwy = mean(hwy)) %>%
  arrange(avg_hwy)

p <- ggplot(mpg, aes(x = displ, y = hwy, color = class)) +
  geom_point(size = 3, alpha = 0.8) +
  geom_smooth(
    aes(group = 1),
    method = "loess",
    formula = y ~ x,
    color = "black",
    linetype = "dashed",
    se = FALSE,
    linewidth = 0.8
  ) +
  scale_color_brewer(palette = "Set2") +
  labs(
    title = "Engine Size vs. Highway Fuel Efficiency",
    subtitle = "Larger engines generally mean lower highway mpg, but class matters a lot",
    x = "Engine Displacement (liters)",
    y = "Highway Miles per Gallon",
    color = "Vehicle Class",
    caption = "Data: ggplot2::mpg"
  ) +
  theme_minimal(base_size = 13) +
  theme(
    plot.title = element_text(face = "bold", size = 16),
    plot.subtitle = element_text(color = "gray40"),
    legend.position = "right",
    panel.grid.minor = element_blank()
  )

print(p)
ggsave("mpg_displ_vs_hwy.png", plot = p, width = 9, height = 6, dpi = 300)
```